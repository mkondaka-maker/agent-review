services:
  - type: web
    name: agent-review-1
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1
    envVars:
      - key: GROQ_MODEL
        value: llama-3.3-70b-versatile
      - key: GROQ_API_KEY
        sync: false
      - key: DATABASE_URL
        value: postgresql://postgres:agent456788990445@db.dqysjythoxcbhmuxyxiy.supabase.co:6543/postgres
